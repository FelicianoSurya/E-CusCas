<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="id.ac.umn.e_cuscas">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECusCas"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".History"
            android:exported="false" />
        <activity
            android:name=".Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Signup_page" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Promo_page" />

        <activity android:name=".DetailOrder" />
        <activity android:name=".DetailOrderTransaction" />
        <activity android:name=".OrderAccept" />
        <activity android:name=".Rating" />

        <activity android:name=".AksesorisDetailOrder" />
        <activity android:name=".AksesorisDetailOrderTransaction" />

        <activity android:name=".OnProgress" />
        <activity android:name=".Login_page" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>